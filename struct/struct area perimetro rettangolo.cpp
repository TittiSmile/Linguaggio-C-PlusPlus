#include <stdio.h>
#include <stdlib.h>
#define MAX 100

struct numeri
{
 int base [MAX];
 int altezza [MAX];
};

struct numeri crea_rettangolo (int base,int altezza);
struct numeri area(struct base,struct altezza );
struct numeri perimetro (struct base,struct altezza);

int main ()
{
    struct a;
    int a1,a2;
    a=area(a1,a2);
    system ("PAUSE");
    return 0;
}

struct numeri crea_rettangolo (struct numeri b,struct numeri a)
{
    struct numeri n;
    n.altezza=altezza;
    n.base=base;
};


struct area(struct b,struct a )
{
    struct numeri n;
    n.altezza=a.altezza*b.base;
    n.base=a.altezza*b.base;

};


